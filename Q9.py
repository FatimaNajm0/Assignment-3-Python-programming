# Define a function that takes one parameter called 'name'
def greet_user(name):
    # Use an f-string to insert the provided name into the greeting message
    print(f"Hello, {name}! Welcome.")

# Call the function and pass a specific name as an argument
greet_user("Ahmed")

# You can call it again with a different name
greet_user("Sara")
